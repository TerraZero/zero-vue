datasource db {
  provider = "sqlite"
  url      = "file:./database/dev.db"
}

generator client {
  provider = "prisma-client-js"
  previewFeatures = ["interactiveTransactions"] /// no without its not working
}
